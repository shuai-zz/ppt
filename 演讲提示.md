# Mission 11 · Milestone 3 最终演示讲稿

建议总时长约 20 分钟。第 10–13 页沿同一次用户任务讲文件调用关系，不展开具体实现；如现场时间紧，可跳过第 14 页的模块边界总结。

## 01 · 把工程做完整（0:40）

开场：前面的 Milestone 已经讲清楚我们要解决什么问题。今天不再重复概念，而是回答一个更工程化的问题：Mission 11 是否已经从“核心路径可用”，走到“功能闭合、质量达标、过程可追溯”。

## 02 · MS3，如何证明做扎实？（0:50）

这一轮我们用三组证据汇报：第一，MVP 的用户链路是否真正闭环；第二，代码与架构是否经得住继续迭代；第三，每次改动是否有 Issue、PR、Review 和文档记录可追溯。

## 03 · 第一章：不是功能多，是闭环完整（0:20）

转场：功能闭合并不等于继续堆功能，而是保证承诺范围内没有断点。

## 04 · 一条主链，四类输入，两类改造（1:10）

说明 MVP 边界。系统接收车辆、玩偶、人形以及开放集模型；围绕“可打印”和“可交互”两类改造，最终输出处理后的零件、预览和制造包。四类输入共享入口与交付契约，但在领域处理阶段走不同路线。

## 05 · 从一个文件，到一套成品（1:20）

沿管线依次讲：上传与检查、结构理解、路线选择、制造几何、结果验证、交付下载。强调每一步都有明确输入输出，用户不需要跳到外部工具补齐流程；失败也会在对应阶段显式返回，而不是产生不可用结果。

## 06 · 模块依赖决定开发顺序（1:05）

这里展示倒排思路：先稳定共享数据契约和几何基础，再完成领域处理包，最后接入编排与界面。这样上层能力建立在已验证的下层模块上，减少后期联动返工，开发进度也能按依赖关系管理。

## 07 · 范围内必须闭合，范围外明确推迟（0:55）

左侧是本轮承诺：上传、分类、真实几何处理、预览和打印包交付。右侧是主动延期项，例如任意骨架、四足姿态、电机控制和打印机闭环。范围控制不是能力不足，而是为了让本轮交付与计划一致。

## 08 · 第二章：主干稳定，能力叠加（0:20）

转场：功能闭环之后，下一问是这套系统能否维护、测试和扩展。

## 09 · Mission 11 系统架构（1:35）

从上到下讲五层：体验层负责上传、选项、预览和下载；任务编排核心维护统一状态与产物契约；领域路由将不同模型分发到车辆、玩偶、人形和开放集处理包；制造几何层提供腔体、关节、布尔、修复和验证等共享能力；底层由存储、测试、CI 与文档支撑。重点是：共享能力下沉，领域策略隔离，新增路线不需要改写整条主链。

## 10 · 一次任务如何穿过仓库（0:55）

承接上一页架构图，从用户上传开始顺着箭头讲：前端发起任务，`routes/pipeline.py` 创建后台 Job，分类器确定类别，Registry 选择领域文件，领域 Pipeline 最终返回零件、预览和压缩包。这里讲清文件职责即可。

## 11 · 同一入口如何分流（0:55）

分类结果进入 Registry 后分别调用车辆、玩偶、人形或 Generic 路线。所有路线实现同一个 `PipelinePlugin` 契约，因此主链只负责 detect、convert 和 result；领域差异留在各自文件中。

## 12 · 共享腔体调用（0:55）

继续沿车辆路线下钻：`cavity_adapter.py` 描述车辆后表面的候选和制造约束，`cavity-engine/engine.py` 统一完成搜索与评分，结果回到 `xatom.py` 执行开腔、盖板和终验。领域知识与通用搜索因此可以独立演进。

## 13 · 关节定位文件链（0:55）

切到人形路线：`auto.py` 给出几何基线，`vision_agent.py` 识别多视图语义并做受限融合，`contracts.py` 输出统一的 `JointPlan`。核心原则是视觉只校准几何基线，制造边界仍由闭合表面、厚度与拓扑决定。

## 14 · 清晰边界，降低联动成本（0:45）

回收前四页代码：API 负责稳定契约，共享核心负责通用几何，领域插件保留车辆、玩偶和人形差异，外部工具通过 Adapter 接入。变化被限制在真正相关的层内。

## 15 · 质量不是最后补一轮（0:55）

质量保证嵌入开发流程：`contracts.py` 守输入输出边界，`tests/` 负责算法和集成回归，`validation/` 检查制造结果，`ci.yml` 在合并前统一执行。质量不是最后集中补救，而是沿调用链逐层拦截。

## 16 · 架构演进有迹可循（0:50）

从早期主干串联，到真实路线、共享几何核心和能力组合，演进方向始终是下沉重复能力、隔离领域差异。架构主干稳定后，新能力以模块方式叠加，没有频繁推倒重来。

## 17 · 第三章：让每次改动都能被解释（0:15）

转场：工程质量不仅体现在代码里，也体现在团队如何提出、评审和沉淀每一次改动。

## 18 · 一次增量的完整生命周期（0:55）

按顺序讲 Proposal、Issue、Branch、PR、AI 检查、人工 Review、Merge。Issue 定义边界和验收标准，PR 只实现对应范围；合并后相关记录仍可回溯。

## 19 · 小步提交，持续互审（0:55）

Issue 承载问题、范围和验收标准；PR 关联 Issue 并描述实现与验证；CI 和人工 Review 共同判断质量；合并后设计变化继续追加记录。右侧按仓库中实际存在的证据类型进行归纳，不强调数量。

## 20 · 项目成品演变（1:30）

第一张是早期原型，证明我们最初已经跑通了可动人形的基本方向；后三张都是 Milestone 3 的实体产物，展示新的模型外观、更多可用姿态和多关节装配。这里要强调的演变不是单纯打印得更精细，而是能力从一次性验证变成可以作用于不同模型的工程流程。

## 21 · 收束（0:30）

结尾：我们本轮完成的不是更多孤立功能，而是三件事——功能形成闭环，质量进入流程，协作留下证据。

## 第 20 页图片说明

- `images/20-initial-prototype.jpg`：初版原型。
- `images/20-ms3-product-a.jpg`：Milestone 3 动态姿态成品。
- `images/20-ms3-product-b.jpg`：Milestone 3 多关节成品。
- `images/20-ms3-product-c.jpg`：Milestone 3 多模型成果。
