# Mission 11 · Milestone 3 最终演示讲稿

建议总时长约 20 分钟。新增代码页只讲接口职责和调用关系，不逐行解释语法；如现场时间紧，可跳过第 14 页的模块边界总结。

## 01 · 把工程做完整（0:40）

开场：前面的 Milestone 已经讲清楚我们要解决什么问题。今天不再重复概念，而是回答一个更工程化的问题：Mission 11 是否已经从“核心路径可用”，走到“功能闭合、质量达标、过程可追溯”。

## 02 · MS3，如何证明做扎实？（0:50）

这一轮我们用三组证据汇报：第一，MVP 的用户链路是否真正闭环；第二，代码与架构是否经得住继续迭代；第三，每次改动是否有 Issue、PR、Review 和文档记录可追溯。

## 03 · 第一章：不是功能多，是闭环完整（0:20）

转场：功能闭合并不等于继续堆功能，而是保证承诺范围内没有断点。

## 04 · 一条主链，四类输入，两类改造（1:10）

说明 MVP 边界。系统接收车辆、玩偶、人形以及开放集模型；围绕“可打印”和“可交互”两类改造，最终输出处理后的零件、预览和制造包。四类输入共享入口与交付契约，但在领域处理阶段走不同路线。

## 05 · 从一个文件，到一套成品（1:20）

沿管线依次讲：上传与检查、结构理解、路线选择、制造几何、结果验证、交付下载。强调每一步都有明确输入输出，用户不需要跳到外部工具补齐流程；失败也会在对应阶段显式返回，而不是产生不可用结果。

## 06 · 模块依赖决定开发顺序（1:05）

这里展示倒排思路：先稳定共享数据契约和几何基础，再完成领域处理包，最后接入编排与界面。这样上层能力建立在已验证的下层模块上，减少后期联动返工，开发进度也能按依赖关系管理。

## 07 · 范围内必须闭合，范围外明确推迟（0:55）

左侧是本轮承诺：上传、分类、真实几何处理、预览和打印包交付。右侧是主动延期项，例如任意骨架、四足姿态、电机控制和打印机闭环。范围控制不是能力不足，而是为了让本轮交付与计划一致。

## 08 · 第二章：主干稳定，能力叠加（0:20）

转场：功能闭环之后，下一问是这套系统能否维护、测试和扩展。

## 09 · Mission 11 系统架构（1:35）

从上到下讲五层：体验层负责上传、选项、预览和下载；任务编排核心维护统一状态与产物契约；领域路由将不同模型分发到车辆、玩偶、人形和开放集处理包；制造几何层提供腔体、关节、布尔、修复和验证等共享能力；底层由存储、测试、CI 与文档支撑。重点是：共享能力下沉，领域策略隔离，新增路线不需要改写整条主链。

## 10 · 模块接口：PipelinePlugin（0:55）

只看三个方法：`category` 声明能力类型，`detect` 输出结构理解结果，`convert` 输出零件和交付物。所有领域模块都遵循同一契约，因此前端和 API 不需要为每种模型重写一套流程；新增路线实现接口并注册即可。

## 11 · 后端入口与调用链（1:00）

上传接口只负责落盘、创建任务并启动后台处理。后台先分类，再通过 Registry 找到领域插件，随后调用 `detect` 或 `convert`；状态统一写回 job store。强调入口层没有混入领域算法，耗时任务也不会阻塞请求。

## 12 · 共享腔体调用（1:00）

左侧是车辆适配器：它只提供候选高度、腔体尺寸和结构墙厚等领域约束。右侧是共享引擎：生成候选、批量测量表面、过滤边缘和深度不安全的位置，再按评分选出最佳曲面。领域知识与通用搜索因此可以独立演进。

## 13 · 关节定位算法（1:10）

关节定位先由网格几何得到稳定基线，再把正面和侧面视觉观测投影回三维。视觉修正会受到最大位移和置信度权重限制，因此不会把语义模型的误差直接写进制造几何。最终输出统一的 `JointPlan`：中心、朝外轴、连接类型与间隙参数。

## 14 · 清晰边界，降低联动成本（0:45）

回收前四页代码：API 负责稳定契约，共享核心负责通用几何，领域插件保留车辆、玩偶和人形差异，外部工具通过 Adapter 接入。变化被限制在真正相关的层内。

## 15 · 质量不是最后补一轮（0:55）

质量保证嵌入开发流程：核心算法持续回归，跨模块主链有集成检查，制造结果还有网格与可装配性终验。代码体现两点：数据契约默认不可随意扩张；几何候选不满足制造约束时直接淘汰，不让错误结果流入交付阶段。

## 16 · 架构演进有迹可循（0:50）

从早期主干串联，到真实路线、共享几何核心和能力组合，演进方向始终是下沉重复能力、隔离领域差异。架构主干稳定后，新能力以模块方式叠加，没有频繁推倒重来。

## 17 · 第三章：让每次改动都能被解释（0:15）

转场：工程质量不仅体现在代码里，也体现在团队如何提出、评审和沉淀每一次改动。

## 18 · 一次增量的完整生命周期（0:55）

按顺序讲 Proposal、Issue、Branch、PR、AI 检查、人工 Review、Merge。Issue 定义边界和验收标准，PR 只实现对应范围；合并后相关记录仍可回溯。

## 19 · 小步提交，持续互审（0:55）

Issue 承载任务和缺陷，PR 保持小范围，Commit Message 说明意图。右侧展示真实 CI 入口：PR 会触发后端测试、CLI 校验、前端类型检查和构建，通过自动门禁后再由成员 Review。

## 20 · 项目成品演变（1:30）

按同一个案例讲四步：原始模型、数字化处理结果、打印零件、最终装配。把前面的接口、算法和质量机制落到实物：系统交付的是能被制造、装配和验证的真实对象。

## 21 · 收束（0:30）

结尾：我们本轮完成的不是更多孤立功能，而是三件事——功能形成闭环，质量进入流程，协作留下证据。

## 图片替换

第 20 页保留了四个同名图片位。建议使用 3:2 横图、宽度不低于 1600 px；拿到照片后直接覆盖即可，HTML 无需修改：

- `images/15-original-model.jpg`
- `images/15-digital-result.jpg`
- `images/15-printed-parts.jpg`
- `images/15-final-assembly.jpg`

四张图推荐保持同一成品、相似取景与统一光线，以便清晰体现“输入 → 数字处理 → 打印零件 → 最终装配”的演变。
